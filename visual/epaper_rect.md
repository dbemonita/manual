# Kotak

Komponen kotak (`rect`) untuk jenis visual `epaper` sama seperti pada komponen kotak (`rect`) jenis visual `hmi`.

Berikut contoh komponen `rect` (alias: `rectangle`):

%[{ 5.1/common/_rect.md }]%
