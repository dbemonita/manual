# Apa itu Laporan?

_Dokumentasi belum tersedia_

#### Petunjuk penggunaan:

Berkas untuk jenis visual ini didefinisikan dengan membuat atribut `type` berisi nilai `report` pada _root tag_ `monita`. Berikut contoh penggunaannya:

```xml
<monita type="report"></monita>
```
