# Garis

Komponen garis (`polyline`) untuk jenis visual `epaper` sama seperti pada komponen garis (`polyline`) jenis visual `hmi`.

Berikut contoh komponen `polyline` (garis)\*:

%[{ common/_polyline.md }]%
